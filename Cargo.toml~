[package]
name = "todo_app"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.39", features = ["cargo"] }
prettytable = "0.10.0"
rusqlite = "0.35.0"

# rusqlite = { version = "", features = ["bundled"] }
